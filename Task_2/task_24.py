# Задача 24: В фермерском хозяйстве в Карелии выращивают чернику. 
# Она растет на круглой грядке, причем кусты высажены только по окружности. 
# Таким образом, у каждого куста есть ровно два соседних. Всего на грядке растет N кустов.
# i ягод.
# Эти кусты обладают разной урожайностью, поэтому ко времени сбора на них
# выросло различное число ягод – на i-ом кусте выросло a
# В этом фермерском хозяйстве внедрена система автоматического сбора черники.
# Эта система состоит из управляющего модуля и нескольких собирающих модулей.
# Собирающий модуль за один заход, находясь непосредственно перед некоторым
# кустом, собирает ягоды с этого куста и с двух соседних с ним.
# Напишите программу для нахождения максимального числа ягод, которое может
# собрать за один заход собирающий модуль, находясь перед некоторым кустом
# заданной во входном файле грядки.

# # Читаем количество кустов и урожайности с каждого куста
# n = int(input())
# a = list(map(int, input().split()))

# # Инициализируем переменную для хранения максимального количества ягод
# max_berries = 0

# # Проходим по каждому кусту и находим максимальное количество ягод, которое может собрать собирающий модуль
# for i in range(n):
#     berries = a[i] + a[(i + 1) % n] + a[(i - 1 + n) % n]
#     max_berries = max(max_berries, berries)

# # Выводим максимальное количество ягод
# print(max_berries)


# В этом коде мы начинаем с чтения количества кустов и урожайности с каждого куста из 
# файла с помощью функции `open()` и метода `readline()`. Мы используем функцию `map()` и 
# функцию `int()` для преобразования строки в список целых чисел, а затем преобразуем список в 
# массив с помощью функции `list()`.
# Затем мы инициализируем переменную `max_berries` для хранения максимального количества 
# ягод и проходим по каждому кусту с помощью цикла `for`. Для каждого куста мы находим количество ягод, 
# которое может быть собрано собирающим модулем с помощью формулы `a[i] + a[(i + 1) % n] + a[(i - 1 + n) % n]`, 
# где `i` - индекс текущего куста, `n` - количество кустов в грядке. Мы используем оператор `%` для обеспечения 
# кругового обхода грядки.
# Наконец, мы записываем максимальное количество ягод в файл с помощью функции `open()` и метода `write()`.

n_bushes = int(input('Введите количество кустов черники: '))
arr = list()
for i in range(n_bushes):
    a =int(input('Введите количество ягод на кусте: '))
    arr.append(a)

arr_count = list()
for i in range(len(arr)):
       arr_count.append(arr[i-2] + arr[i-1] + arr[i])
print(max(arr_count))